public class Pikachu extends Pokemon
{
    //Global variables;
    //if you define the same variable in a sub-class, it will be a different value assigned to the same variable
    public Pikachu(){
        setHp(35);
        setAttack(55);
        setDefense(30);
        setSpeed(90);
    }
    // could make another constructor with all values as parameters
    
    
}